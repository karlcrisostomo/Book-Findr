import React from "react";
import { Link } from "react-router-dom";
import {  } from "../asset";

const Logo = () => {
  return (
    <Link to="/">
      -Reads
    </Link>
  );
};

export default Logo;
